import type { ReactNode } from 'react';

/** Native/stub: children render inline (native menus position over content via zIndex). */
export function renderInBody(children: ReactNode): ReactNode {
  return children;
}